// Export all enums from models
export * from './models';

// App-wide enums
export enum Currency {
  TRY = 'TRY',
  USD = 'USD',
  EUR = 'EUR',
  GBP = 'GBP',
  JPY = 'JPY',
  CHF = 'CHF',
  AUD = 'AUD',
  CAD = 'CAD',
}

export enum AppTheme {
  Light = 'light',
  Dark = 'dark',
  Auto = 'auto',
}

export enum Language {
  TR = 'tr',
  EN = 'en',
}

